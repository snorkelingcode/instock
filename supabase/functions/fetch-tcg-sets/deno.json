
{
  "tasks": {
    "start": "deno run --allow-net --allow-env index.ts",
    "deploy": "deployctl deploy --project=fetch-tcg-sets index.ts"
  },
  "compilerOptions": {
    "allowJs": true,
    "lib": ["deno.window"]
  }
}
